# CMake package configuration file for the SofaOROpenCV plugin

@PACKAGE_INIT@

find_package(SofaFramework REQUIRED)

if(NOT TARGET SofaOROpenCV)
	include("${CMAKE_CURRENT_LIST_DIR}/SofaOROpenCVTargets.cmake")
endif()

check_required_components(SofaOROpenCV)
