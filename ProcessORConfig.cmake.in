# CMake package configuration file for the ProcessOR plugin

@PACKAGE_INIT@

find_package(SofaFramework REQUIRED)

if(NOT TARGET ProcessOR)
	include("${CMAKE_CURRENT_LIST_DIR}/ProcessORTargets.cmake")
endif()

check_required_components(ProcessOR)

get_filename_component(ProcessOR_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(ProcessOR_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
set(ProcessOR_LIBRARIES ProcessOR)
